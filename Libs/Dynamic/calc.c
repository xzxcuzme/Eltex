#include <stdio.h>

int sum(int x, int y)
{
	int k=x+y;
	return k;
}

int diff(int x, int y)
{
	int k=x-y;
	return k;
}

int inc(int x, int y)
{
	int k=x*y;
	return k;
}

int divn(int x, int y)
{
	int k=x/y;
	return k;
}